---
name: Build docker image - workflow_call/on-demand

'on':
  workflow_call:
    secrets:
      DOCKER_PROVIDER:
        required: true
      DOCKER_USERNAME:
        required: true
      DOCKER_PASSWORD:
        required: true
      DOCKER_REPO:
        required: true
    inputs:
      toolchain:
        type: string
        description: 'Rust toolchain'
        default: stable
      version:
        type: string
        description: 'build tag/version'
      tag_alias:
        type: string
        description: 'build tag alias'
      build_items:
        type: string
        default: tari-ootle_all
        description: 'build images'
      platforms:
        type: string
        # linux/arm64, linux/amd64, linux/riscv64
        default: linux/amd64
        description: 'docker target build platform(s)'

env:
  IMAGE_NAME: ootle
  DAYS_to_EXPIRE: 30

permissions:
  contents: read

jobs:
  builds_envs_setup:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}

    steps:
      - name: checkout tari-ootle
        uses: actions/checkout@v5

      - name: Set Matrix
        id: set-matrix
        shell: bash
        run: |
          # set -xuo pipefail
          build_items=${{ inputs.build_items }}
          if [ -z "${{ inputs.version }}" ]; then
            echo "Get tari version"
            TARI_SOURCE_ROOT="./"
            VAPP=$(awk -F ' = ' '$1 ~ /^version/ \
              { gsub(/["]/, "", $2); printf("%s",$2) }' \
              "${TARI_SOURCE_ROOT}/Cargo.toml")
            VBRANCH=$(git --git-dir ${TARI_SOURCE_ROOT}/.git branch --show-current)
            VSHA_SHORT=$(git --git-dir ${TARI_SOURCE_ROOT}/.git rev-parse --short HEAD)
            VERSION="v${VAPP}_${VBRANCH}_$(date -u '+%Y%m%d')_${VSHA_SHORT}"
          else
            VERSION=${{ inputs.version }}
          fi

          cd buildtools/docker_rig
          source ./build-matrix.sh "${{ inputs.build_items }}" "${VERSION}" "${{ inputs.platforms }}"

          echo "${matrix}"
          echo "${matrix}" | jq .
          echo "matrix=${matrix}" >> $GITHUB_OUTPUT

  docker_builds:
    name: Docker ${{ matrix.builds.arch }} ${{ matrix.builds.image_name }} build
    needs: builds_envs_setup

    continue-on-error: true

    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.builds_envs_setup.outputs.matrix) }}

    permissions:
      contents: read
      packages: write

    runs-on: ${{ matrix.builds.runner }}

    steps:
      - name: checkout repo
        uses: actions/checkout@v5

      - name: Setup expiration for none releases
        shell: bash
        run: |
          if [[ "${{ github.ref }}" =~ ^refs\/tags\/v[0-9]+\.[0-9]+\.[0-9]+.*$ ]]; then
            echo "No Expire for release"
          else
            echo "Expires in ${{ env.DAYS_to_EXPIRE }} days"
            echo "EXPIRATION=${{ env.DAYS_to_EXPIRE }}d" >> $GITHUB_ENV
          fi

      - name: environment setup
        shell: bash
        run: |
          IMAGE_NAME=${{ matrix.builds.image_name }}
          if [ -z "${IMAGE_NAME}" ] || [ "${IMAGE_NAME}" = "ootle" ]; then
            echo "tari builds"

            if [ ! -z "${{ inputs.tag_alias }}" ]; then
              echo "Setup tag_alias with network"
              echo "TAG_ALIASQ=${{ secrets.DOCKER_PROVIDER }}/${{ secrets.DOCKER_REPO }}/${{ matrix.builds.image_name }}:${{ inputs.tag_alias }}-${{ matrix.builds.arch }}" >> $GITHUB_ENV
              echo "TAG_ALIASG=ghcr.io/${{ github.repository_owner }}/${{ matrix.builds.image_name }}:${{ inputs.tag_alias }}-${{ matrix.builds.arch }}" >> $GITHUB_ENV
            fi
            echo "DSuffix=-${{ matrix.builds.arch }}" >> $GITHUB_ENV
          fi
          # What about riscv?
          if [[ "${{ matrix.builds.arch }}" == "amd64" ]]; then
            echo "cargo_arch=x86-64" >> $GITHUB_ENV
          elif [[ "${{ matrix.builds.arch }}" == "arm64" ]]; then
            echo "cargo_arch=aarch64" >> $GITHUB_ENV
          else
            echo "Other platforms are not yet supported!"
            exit 1
          fi

      - name: Set up QEMU for Docker
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        env:
          # DOCKER_METADATA_ANNOTATIONS_LEVELS: manifest,index
          DOCKER_METADATA_ANNOTATIONS_LEVELS: manifest
        with:
          images: |
            # name/${{ matrix.builds.image_name }}
            ghcr.io/${{ github.repository_owner }}/${{ matrix.builds.image_name }}
            ${{ secrets.DOCKER_PROVIDER }}/${{ secrets.DOCKER_REPO }}/${{ matrix.builds.image_name }}
          tags: |
            type=schedule
            type=ref,event=tag
            type=ref,event=branch,enable=${{ startsWith(matrix.builds.image_name, 'ootle') }}
            type=ref,event=pr,enable=${{ startsWith(matrix.builds.image_name, 'ootle') }}
            type=semver,pattern={{version}},enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            type=sha
          labels: |
            maintainer=${{ github.actor }}
            quay.expires-after=${{ env.EXPIRATION }}
            org.opencontainers.image.vendor=TariLabs
            org.opencontainers.image.title=${{ matrix.builds.image_name }}
            org.opencontainers.image.description=Tari OOTLE docker image ${{ matrix.builds.image_name }} for ${{ matrix.builds.arch }}
            org.opencontainers.image.url=https://github.com/${{ github.repository }}
            org.opencontainers.image.source=https://github.com/${{ github.repository }}.git#${{ github.sha }}
            org.opencontainers.image.version=${{ matrix.builds.version }}
          annotations: |
            org.opencontainers.image.vendor=TariLabs
            org.opencontainers.image.title=${{ matrix.builds.image_name }}
            org.opencontainers.image.description=Tari OOTLE docker image ${{ matrix.builds.image_name }} for ${{ matrix.builds.arch }}
            org.opencontainers.image.url=https://github.com/${{ github.repository }}
            org.opencontainers.image.source=https://github.com/${{ github.repository }}.git#${{ github.sha }}
          flavor: |
            # latest=${{ startsWith(github.ref, 'refs/tags/v') }}
            latest=false
            suffix=${{ env.DSuffix }}

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          # username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Login to Docker Image Provider
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.DOCKER_PROVIDER }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Docker image build and push
        id: docker_build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/${{ matrix.builds.dockerfile }}
          platforms: ${{ matrix.builds.platform }}
          push: true
          provenance: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            VERSION=${{ matrix.builds.version }}
          tags: |
            ${{ secrets.DOCKER_PROVIDER }}/${{ secrets.DOCKER_REPO }}/${{ matrix.builds.image_name }}:${{ matrix.builds.version }}${{ env.DSuffix }}
            ghcr.io/${{ github.repository_owner }}/${{ matrix.builds.image_name }}:${{ matrix.builds.version }}${{ env.DSuffix }}
            ${{ steps.meta.outputs.tags }}
            ${{ env.TAG_ALIASQ }}
            ${{ env.TAG_ALIASG }}
          labels: ${{ steps.meta.outputs.labels }}
          annotations: ${{ steps.meta.outputs.annotations }}

      - name: Image digest
        run: |
          echo "Docker Digest - ${{ steps.docker_build.outputs.digest }}"
          echo "Docker Image - ${{ matrix.builds.image_name }}:${{ matrix.builds.version }}${{ env.DSuffix }}"

      - name: Debug meta labels
        run: echo "${{ steps.meta.outputs.labels }}"
      - name: Debug meta annotations
        run: echo "${{ steps.meta.outputs.annotations }}"

  multi-arch-create-manifest:
    needs: [ builds_envs_setup, docker_builds ]
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Log in to Registries
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login ${{ secrets.DOCKER_PROVIDER }} -u ${{ secrets.DOCKER_USERNAME }} --password-stdin

      - name: Pre for multi-arch image merge
        env:
          BMATRIX: "${{ needs.builds_envs_setup.outputs.matrix }}"
        run: |
          set -xuo pipefail
          echo '${{ env.BMATRIX }}' | jq -r

          # Get all multi-arch image_names (have both amd64 and arm64), with version
          mapfile -t multiarch_images < <(
            echo '${{ env.BMATRIX }}' | jq -r '
              .builds
                | group_by([.image_name])
                | map(select((map(.arch) | index("amd64")) and (map(.arch) | index("arm64"))))
                | map({image_name: .[0].image_name, version: .[0].version})
                | unique
                | map("\(.image_name):\(.version)")
                | .[]
              '
            )

          if [ ${#multiarch_images[@]} -eq 0 ]; then
            echo "No multi-arch Docker images found."
            mapfile -t all_images < <(
              echo '${{ env.BMATRIX }}' | jq -r '.builds[].image_name' | sort -u)
                for img in "${all_images[@]}"; do
                  if [[ ! " ${multiarch_images[*]} " =~ " ${img} " ]]; then
                    echo "Image '$img' has only one architecture built."
                  fi
                done
          else
            echo "Found ${#multiarch_images[@]} multi-arch Docker images."
            echo "${multiarch_images[*]}"
            for DENTRY in "${multiarch_images[@]}"; do
              DNAME="${DENTRY%%:*}"             # before first :
              REST="${DENTRY#*:}"               # drop first :
              DVERSION="${REST%%:*}"            # version
              DSUFFIX=""

              echo "Multi-arch ${DNAME} processing ..."
              for DREGISTRY in ghcr.io quay.io; do
                if [[ "${DREGISTRY}" == "ghcr.io" ]]; then
                  REPO="${{ github.repository_owner }}"
                else
                  REPO="${{ secrets.DOCKER_REPO }}"
                fi

                # Convert GitHub expressions into shell variables first
                GH_REPO="${{ github.repository }}"
                GH_ACTOR="${{ github.actor }}"
                GH_SHA="${{ github.sha }}"

                # Arrays mangle quoted strings
                ## --annotation "index:org.opencontainers.image.description='Tari OOTLE docker image'"
                DANNOTATIONS=(
                  --annotation "index:org.opencontainers.image.title=${DNAME}"
                  --annotation "index:org.opencontainers.image.vendor=TariLabs"
                  --annotation "index:org.opencontainers.image.url=https://github.com/${GH_REPO}"
                  --annotation "index:org.opencontainers.image.source=https://github.com/${GH_REPO}.git#${GH_SHA}"
                  --annotation "index:org.opencontainers.image.version=${DVERSION}"
                  --annotation "index:org.opencontainers.image.licenses=BSD-3-Clause"
                  --annotation "index:org.opencontainers.image.revision=${GH_SHA}"
                  --annotation "index:maintainer=${GH_ACTOR}"
                )

                if [[ "${{ github.ref }}" =~ ^refs\/tags\/v[0-9]+\.[0-9]+\.[0-9]+.*$ ]]; then
                  echo "No Expire for release"
                else
                  if [[ "${DREGISTRY}" == "quay.io" ]]; then
                    QExpire="${{ env.DAYS_to_EXPIRE }}"
                    echo "Expires in ${QExpire} days"
                    DANNOTATIONS+=(
                      --annotation "index:quay.expires-after=${QExpire}d"
                    )
                  fi
                fi

                echo "Registry run - ${DREGISTRY}"
                echo "Creating multi-arch image for ${REPO}/${DNAME} for ${DVERSION}${DSUFFIX}"

                docker buildx imagetools create \
                  --tag ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX}-a \
                    --annotation 'org.opencontainers.image.description="Tari '${DNAME}' docker image"' \
                    ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX}-amd64 \
                    ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX}-arm64 || true

                docker buildx imagetools create \
                  --tag ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX}-b \
                    --annotation 'index:org.opencontainers.image.description="Tari '${DNAME}' docker image"' \
                    ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX}-amd64 \
                    ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX}-arm64 || true

                docker buildx imagetools create \
                  --tag ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX}-c \
                    --annotation 'manifest:org.opencontainers.image.description="Tari '${DNAME}' docker image"' \
                    ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX}-amd64 \
                    ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX}-arm64 || true

                docker buildx imagetools create \
                  --tag ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX} \
                    --annotation 'index:org.opencontainers.image.description="Tari '${DNAME}' docker image"' \
                    ${DANNOTATIONS[@]} \
                    ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX}-amd64 \
                    ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX}-arm64 || true

                echo "Inspect multi-arch image for ${REPO}/${DNAME}"
                docker manifest inspect ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX} > manifest.json
                cat manifest.json
                docker buildx imagetools inspect ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX} > buildx-manifest.json
                cat buildx-manifest.json

                if [[ "${{ inputs.tag_alias }}" =~ ^latest-.*$ ]]; then
                  echo "Latest Alias Multi-Arch tag - ${{ inputs.tag_alias }}"
                  docker buildx imagetools create \
                    --tag ${DREGISTRY}/${REPO}/${DNAME}:${{ inputs.tag_alias }}${DSUFFIX} \
                      --annotation 'index:org.opencontainers.image.description="Tari '${DNAME}' docker image"' \
                      ${DANNOTATIONS[@]} \
                      ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX}-amd64 \
                      ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX}-arm64 || true
                fi

                if [[ "${GITHUB_REF}" =~ ^refs\/tags\/v[0-9]+\.[0-9]+\.[0-9]+.*$ ]]; then
                  echo "Release Multi-Arch Tag - ${GITHUB_REF_NAME} for ${GITHUB_REF}"
                  docker buildx imagetools create \
                    --tag ${DREGISTRY}/${REPO}/${DNAME}:${GITHUB_REF_NAME} \
                      --annotation 'index:org.opencontainers.image.description="Tari '${DNAME}' docker image"' \
                      ${DANNOTATIONS[@]} \
                      ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX}-amd64 \
                      ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX}-arm64  || true
                fi

                if [[ "$GITHUB_REF_TYPE" == "tag" && "$GITHUB_REF_NAME" =~ ^v[0-9]+\.[0-9]+\.[0-9]+.*$ ]]; then
                  echo "Latest Release Multi-Arch tag - $GITHUB_REF_NAME for $GITHUB_REF using ${DVERSION}${DSUFFIX}"
                  docker buildx imagetools create \
                    --tag ${DREGISTRY}/${REPO}/${DNAME}:latest${DSUFFIX} \
                      --annotation 'index:org.opencontainers.image.description="Tari '${DNAME}' docker image"' \
                      ${DANNOTATIONS[@]} \
                      ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX}-amd64 \
                      ${DREGISTRY}/${REPO}/${DNAME}:${DVERSION}${DSUFFIX}-arm64 || true
                fi
              done
            done
          fi
